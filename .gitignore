# Artefacts de build (regénérés par build.ps1 / build.sh)
build/
*.class
*.jar

# ...sauf les binaires de release versionnés (pour télécharger sans compiler)
!*/dist/*.jar

# Sauvegardes du jar serveur
*.bak
*.bak-*
*.bak.*

# Divers OS / éditeur
Thumbs.db
.DS_Store
